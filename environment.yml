# Environment for Alice TRF analysis
# usage: $ conda env create --file=environment.yml
name: eelbrain
channels:
- conda-forge
dependencies:
- eelbrain >= 0.40
- pip
- ipython
- jupyter
- ipywidgets
- jupytext
- seaborn
- pip:
  - gammatone
  - https://github.com/Hugo-W/pyEEG/archive/refs/heads/master.zip
